{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Android gradle build",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/source/android/"
            },
            "command": "gradlew app:assemble",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}